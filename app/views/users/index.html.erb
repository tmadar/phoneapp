<div class="navbar navbar-fixed-top">
	<div class="navbar-inner">
		<div class="container">
			<ul class="nav">
				<li>
					<%= link_to "Home", home_index_path, :method => :get%>
				</li>
				<li class="active">
					<%= link_to "Agent Settings", users_index_path, :method => :get%></a>
				</li>
				<li>
					<%= link_to "Log Out", destroy_user_session_url, :method => :delete %>
				</li>
			</ul>
		</div>
	</div>
</div>

<div class="content">
	<h1>Agent Settings</h1>
	<br>
	<br>
	<br>
	
	<% @users.each do |user|%>
	<table class="table table-striped table-bordered table-condensed">
		<tr>
			<th style="width: 10%">Name:</th>
			<th style="width: 30%">Callback #:</th>
			<th style="width: 30%">Email:</th>
			<th style="width: 15%">Means of Contact:</th>
			<th style="width: 15%">Available:</th>
		</tr>
		<tr>
			<td><%= user.name %></td>
			<td><%= user.phone %></td>
			<td><%= user.email %></td>
			<td>
				<select name="means_of_contact">
					<option value="Phone">Phone</option>
					<option value="Text">Text</option>
				</select>
			</td>
			<td>
				<select name="availability">
					<option value="Yes">Open</option>
					<option value="No">No</option>
				</select>
			</td>
		</tr>
	</table>
	<%= link_to "Edit Agent", edit_user_path(:id => user.id) %>
	<% end %>
	
	
</div>
