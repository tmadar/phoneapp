<div class="navbar navbar-fixed-top">
	<div class="navbar-inner">
		<div class="container">
			<ul class="nav">
				<li class="active">
					<%= link_to "Home", home_index_path, :method => :get%>
				</li>
				<li>
					<%= link_to "Agent Settings", users_index_path, :method => :get%></a>
				</li>
				<li>
					<%= link_to "Log Out", destroy_user_session_url, :method => :delete %>
				</li>
			</ul>
		</div>
	</div>
</div>

<div class="content">
	<h1>Levion Automated Telephone System Home Page</h1>
	<br>
	<br>
	<br>
	<br>
	<% @calls.reverse.each do |call| %>
		<table class="table table-striped table-bordered table-condensed">
			<tr>
				<th style="width: 30%">Time:</th>
				<th style="width: 10%">Duration:</th>
				<th style="width: 30%">Callback #:</th>
				<th style="width: 10%">Call Status:</th>
				<th style="width: 20%">Disposition:</th>
			</tr>
			<tr>
				<td><%= call.received_at %></td>
				<td><%= call.duration %></td>
				<td><%= call.caller %></td>
				<td><%= call.status %></td>
				<td>
					<select name="choose">
						<option selected>Currently <%= call.disposition %></option>
						<option value="Open">Open</option>
						<option value="Closed">Closed</option>
						<option value="Unresponsive">Unresponsive</option>
						<option value="Rescheduled">Rescheduled</option>
					</select>
				</td>
			</tr>
		</table>
		<%= link_to('View Details',  records_path(:id => call.id), :method => :get, :class => "popover-comment", :rel => "popover", :title => "Recent Comment", :"data-content" => "#{call.last_comment}")%>
		<br>
		<br>
		<br>
		<br>
	<% end %>
</div>
